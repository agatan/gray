def early(b: Bool) -> Int {
    let x = if b {
        true
    } else {
        return 0
    }
    if x {
        1
    } else {
        1
    }
}

def main() {
    print_int(early(true))
    print_int(early(false))
}
